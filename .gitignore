node_modules/
dist/
*.local
.DS_Store

# Claude Code: keep local session data (.env, .teams_request) out, but track
# shared config (skills, rules, the handoff doc).
.claude/*
!.claude/skills/
!.claude/rules/
!.claude/handoff.md

# Local Quest headset debug log (dev-only)
quest-debug.log

# Local Codex launcher + config
/codex.sh
/.codex/config.toml
